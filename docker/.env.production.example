# ============================================
# Aegis NGFW - Production Environment
# Copy to .env.production and fill in values
# ============================================

# ── Domain & TLS ───────────────────────────────
DOMAIN=firewall.example.com
CERTBOT_EMAIL=admin@example.com

# ── Database (CHANGE THESE!) ───────────────────
POSTGRES_DB=aegis_ngfw
POSTGRES_USER=aegis
POSTGRES_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD_HERE

# ── JWT Secret (min 32 chars, CHANGE THIS!) ────
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_48

# ── Backup ─────────────────────────────────────
BACKUP_RETENTION_DAYS=30
BACKUP_SCHEDULE=0 2 * * *

# ── Agent ──────────────────────────────────────
AGENT_SECRET_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32
