# ============================================================
# Aegis NGFW Agent Configuration
# Copy to /opt/aegis/.env and update values
# ============================================================

# PostgREST API URL (required)
# If using docker-compose on the same host:
API_URL=http://localhost:8080/api

# Agent secret key (optional, for extra auth)
AGENT_SECRET_KEY=

# Intervals (seconds)
SYNC_INTERVAL=60
METRICS_INTERVAL=30

# Firewall backend: ipt (iptables) or nft (nftables, recommended)
FIREWALL_BACKEND=nft

# Hostname identifier
HOSTNAME_ID=aegis-fw-01

# Interface mapping: DB name → Linux device name
# Adjust to match your server's network interfaces
IFACE_WAN=eth0
IFACE_LAN=eth1
IFACE_DMZ=eth2
IFACE_GUEST=eth3
IFACE_WAN2=eth4

# Agent directory
AEGIS_DIR=/opt/aegis
