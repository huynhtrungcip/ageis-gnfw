[Unit]
Description=Aegis NGFW Agent - System Monitor & Firewall Sync
Documentation=https://github.com/aegis-ngfw
After=network-online.target docker.service
Wants=network-online.target

[Service]
Type=simple
User=root
Group=root
WorkingDirectory=/opt/aegis
EnvironmentFile=/opt/aegis/.env
ExecStart=/opt/aegis/aegis-agent.sh daemon
ExecReload=/bin/kill -HUP $MAINPID
Restart=always
RestartSec=10
StandardOutput=append:/opt/aegis/agent.log
StandardError=append:/opt/aegis/agent.log
TimeoutStopSec=15

# Security hardening
ProtectSystem=strict
ReadWritePaths=/opt/aegis /etc/suricata/rules /var/lib/dhcp
CapabilityBoundingSet=CAP_NET_ADMIN CAP_NET_RAW CAP_SYS_ADMIN
AmbientCapabilities=CAP_NET_ADMIN CAP_NET_RAW CAP_SYS_ADMIN
NoNewPrivileges=false

[Install]
WantedBy=multi-user.target
